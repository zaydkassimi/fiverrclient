import React from 'react'
import Invoicing from '../invoicing'

// Reuse the existing invoicing page UI but expose it under /sales (renamed)
export default function SalesPage() {
  return <Invoicing />
}


